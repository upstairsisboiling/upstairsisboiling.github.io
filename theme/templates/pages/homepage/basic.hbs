{{#>base page="homepage"}}
  {{#>layouts/single-column}}
    <header {{region "header"}}>
      {{>components/header}}
    </header>

    <main {{region "main"}}>
      {{> @partial-block }}
      {{>components/home-listing}}
    </main>
  {{/layouts/single-column}}
{{/base}}
